{
  "name": "server",
  "version": "1.0.0",
  "scripts": {
    "watch": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "gulp": "^3.9.1",
    "gulp-rsync": "0.0.6",
    "gulp-watch": "^4.3.10"
  }
}
